import React from 'react'

type Props = {}

const page = (props: Props) => {
  //authentication


  //if account alr exists, redirect


  //if account does not exist, we need to create the account in the database

  return (
    <div>page</div>
  )
}

export default page